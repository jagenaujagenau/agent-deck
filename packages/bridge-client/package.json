{
  "name": "@agent-control-dashboard/bridge-client",
  "version": "0.1.0",
  "description": "Consumer SDK for the Agent Deck bridge: the live stream, the policies, and the verbs, so an interface can be built from the wire contract alone.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
